import * as colors from "./colors";
import { GlobalStyle } from "./GlobalStyle";
import * as sizes from "./sizes";
import * as times from "./times";

export const theme = { colors, GlobalStyle, sizes, times };
